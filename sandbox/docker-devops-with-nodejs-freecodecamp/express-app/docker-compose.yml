version: "3"
services:
  my-node-app-2222:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "4000:3000"
    volumes:
      - ./:/usr/src/app
    environment:
      - PORT=3000
